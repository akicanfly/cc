/**
 * STRIPPED: initializeAnalyticsSink / initializeAnalyticsGates are no-ops.
 * No Datadog, no 1P event logging in this build.
 */

export function initializeAnalyticsGates(): void {
  // no-op
}

export function initializeAnalyticsSink(): void {
  // no-op
}
