export class GoogleAuth {
  constructor(opts) {
    throw new Error(
      'google-auth-library is not included in this build. To use Vertex AI, install the full @anthropic-ai/claude-code package.'
    )
  }
}
